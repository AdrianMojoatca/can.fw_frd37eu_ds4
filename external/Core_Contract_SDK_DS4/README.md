# ds4_contract

Single source of truth for the DS4 CORE<->FW ABI/contract (Core_Contract_SDK_DS4).
Consumed as a git submodule at external/Core_Contract_SDK_DS4 by both core_ds4 (fixed CORE) and fw_frd37eu (FW). Edit here only.
